@model GCRBA.Models.User

@{
    ViewBag.Title = "NonMember";
}

@if (Model.isMember == 1)
{
    if (Model.isAdmin == 1)
    {
        <p>This page is for non</p>
    }
    <p>This page is for non-members only.</p>
}
else
{
    <div>
        <button class="button-action" id="btnJoin" name="btnSubmit" value="join">Join the GCRBA!</button>
    </div>

    <p>Hello,  @Model.FirstName, you are logged in as a non-member.</p>
}

